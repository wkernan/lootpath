-- Lootpath/UI/MainFrame.lua (stub; built in M2-2, WKE-520)
-- Native frames only (no AceGUI). The paste editbox, the Equip Now panel and
-- the Settings API options page land here.
local _, ns = ...

ns.UI = {}

function ns.UI.Toggle()
    ns.Log("the frame arrives in M2-2 (WKE-520). Until then: /lootpath help.")
end
